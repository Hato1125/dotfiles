# RTK 規則

shell でコマンドを実行するときに適用する。

原則: **RTK が対応するコマンドは RTK 経由で実行する。**

- `git status` は `rtk git status`、`cargo test` は `rtk cargo test` のように実行する。
- テスト、ビルド、lint、ログ、検索など、出力が多いコマンドでは特に優先する。
- RTK に対応するサブコマンドがない場合は、通常のコマンドを使用する。
- 未加工の出力が必要な場合は、RTK を使用しない。
